version: "3.9"
services:
  postgres:
    container_name: assets-service-database
    image: postgres
    environment:
      POSTGRES_USER: "${DATABASE_USER}"
      POSTGRES_PASSWORD: "${DATABASE_PASSWORD}"
      POSTGRES_MULTIPLE_DATABASES: "${DATABASE_NAME_PREFIX}_1,${DATABASE_NAME_PREFIX}_2"
    volumes:
      - ./scripts/docker-postgresql-multiple-databases:/docker-entrypoint-initdb.d
      - database-volume:/var/lib/postgresql/data

  liquibase_shard_1:
    image: liquibase/liquibase
    volumes:
      - ./docker/liquibase/migrations:/liquibase/changelog
    command:
     - /bin/sh
     - -c
     - |
        echo "sleep 5s"
        sleep 5
        liquibase --username=${DATABASE_USER} --password=${DATABASE_PASSWORD} --changeLogFile=changelog/db.changelog-root.xml --url=${DATABASE_URL}_1 update
    depends_on:
      - postgres
    restart: on-failure
    healthcheck:
      test: curl -sS http://127.0.0.1 || exit 1
      interval: 5s
      timeout: 10s
      retries: 3

  liquibase_shard_2:
    image: liquibase/liquibase
    volumes:
      - ./docker/liquibase/migrations:/liquibase/changelog
    command:
     - /bin/sh
     - -c
     - |
        echo "sleep 5s"
        sleep 5
        liquibase --username=${DATABASE_USER} --password=${DATABASE_PASSWORD} --changeLogFile=changelog/db.changelog-root.xml --url=${DATABASE_URL}_2 update
    depends_on:
      - postgres
    restart: on-failure
    healthcheck:
      test: curl -sS http://127.0.0.1 || exit 1
      interval: 5s
      timeout: 10s
      retries: 3

  api:
    build: .
    container_name: assets-service-api
    ports:
      - "3005:3005"
    depends_on:
      - liquibase_shard_1
      - liquibase_shard_2
      - postgres
    restart: always
    healthcheck:
      test: curl -sS http://127.0.0.1 || exit 1
      interval: 5s
      timeout: 10s
      retries: 3

volumes:
  database-volume:
networks:
  default:
    name: clearway-task-assets-service-network
